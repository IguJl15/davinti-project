
spring:
  jpa:
    show-sql: true
    database: postgresql
    hibernate:
      ddl-auto: update
  flyway:
    schemas: public
    driver-class-name: org.postgresql.Driver
  datasource:
    url: jdbc:postgresql://0.0.0.0:5432/mydatabase
    username: postgres
    password: postgres

